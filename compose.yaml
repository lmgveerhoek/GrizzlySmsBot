services:
  grizzlysms-bot:
    build: .
    container_name: grizzlysms-bot
    restart: "no"
    init: true
    env_file:
      - .env
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    volumes:
      - grizzlysms-state:/data

volumes:
  grizzlysms-state:
